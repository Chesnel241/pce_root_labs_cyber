// cross-env/src/index.js — paquet LÉGITIME (pour comparaison lors de l'audit).
// Aucun script de cycle de vie (pas de postinstall), aucune lecture de secrets,
// aucun appel réseau. C'est la dépendance que le projet voulait réellement.
'use strict';

const crossSpawn = require('cross-spawn');

module.exports = function crossEnv(args, options = {}) {
  // (Implémentation simplifiée pour le lab.)
  return crossSpawn;
};
