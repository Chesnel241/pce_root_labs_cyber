whiteout: ce marqueur (.wh.<nom>) signale que /app/deploy-creds.env est
"supprimé" dans la couche fusionnée finale (overlay). MAIS le fichier reste
intact dans la couche 04 (layers/04/app/deploy-creds.env) : la suppression
overlay n'efface JAMAIS le contenu d'une couche antérieure.
